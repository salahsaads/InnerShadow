/// A Flutter widget that renders an inner shadow effect on its child widget.
///
/// Usage:
/// ```dart
/// InnerShadow(
///   blur: 10,
///   color: Colors.black38,
///   offset: Offset(10, 10),
///   child: Container(
///     width: 200,
///     height: 200,
///     color: Colors.white,
///   ),
/// )
/// ```
library inner_shadow_flutter;

export 'src/inner_shadow.dart';
