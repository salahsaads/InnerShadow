name: inner_shadow_example
description: Example app demonstrating the inner_shadow_flutter package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  inner_shadow_flutter:
    path: ../

flutter:
  uses-material-design: true
